import { Theme } from 'styled-components'

export const DEFAULT_THEME: Theme = {
    font: {
        primary: 'Gilroy, sans-serif',
    },
    weight: {
        regular: 400,
        medium: 500,
        semiBold: 600,
        bold: 700,
    },
}
